class Solution:
    def minOperations(self, nums: List[int]) -> int:
        t=0
        for i in range(len(nums) - 2):
            if nums[i]==0:
                t+=1
                nums[i]=1
                nums[i+1]=abs(nums[i+1]-1)
                nums[i+2]=abs(nums[i+2]-1)
            

       
        return t if all (x==1 for x in nums ) else -1
