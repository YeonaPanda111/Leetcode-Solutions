class Solution:
    def findIntersectionValues(self, nums1: List[int], nums2: List[int]) -> List[int]:
        n=0
        m=0
        for i in nums1:
            if i in nums2:
                n+=1

        for i in nums2:
            if i in nums1:
                m+=1
        return [n,m]
