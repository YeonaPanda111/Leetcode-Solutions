class Solution:
    def findMatrix(self, nums: List[int]) -> List[List[int]]:
        ans=[]
        while (nums):
            L=[]
            for i in nums:
                if i not in L:
                    L.append(i)

            ans.append(L)
            for i in L:
                nums.remove(i)
        return ans
        
