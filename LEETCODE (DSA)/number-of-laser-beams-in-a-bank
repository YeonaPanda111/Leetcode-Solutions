class Solution:
    def numberOfBeams(self, bank: List[str]) -> int:
        c=0
        for i in range(len(bank)):
            for j in bank[i+1:]:
                if j.count("1")!=0:
                    c+=bank[i].count("1")*j.count("1")
                    break
                

        return c
