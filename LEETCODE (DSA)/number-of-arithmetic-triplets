class Solution:
    def arithmeticTriplets(self, nums: List[int], diff: int) -> int:
        t=0
        for i in nums:
            if (i+diff) in nums and (i+2*diff) in nums:
                t+=1
        return t
