class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums.sort()
        if target not in nums:
            return []
        ind=nums.index(target)
        t=nums.count(target)
        ans=[ind]
        for i in range(1,t):
            ans.append(ind+i)

        return ans
