class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        maxi=0
        l=[]
        for i in sentences:
            l=[x for x in i.split()]
            
            if maxi<len(l):
                maxi=len(l)


        return maxi
