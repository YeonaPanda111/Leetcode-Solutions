class Solution:
    def findDifferentBinaryString(self, nums: List[str]) -> str:
        n=len(nums[0])
        for i in range(0,(2**n)):
            t=bin(i)[2: ]

            if len(t) < n:
                t = "0" * (n - len(t)) + t
            if t not in nums:
                return t
